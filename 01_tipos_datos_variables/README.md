# 01 - Tipos de Datos y Variables en Java

Este módulo reúne los primeros conceptos vistos en el curso **TodoCode Academy** y mis prácticas personales.  
Incluye teoría básica, ejemplos y ejercicios.

---

## 📖 Tipos de datos en Java
- **Primitivos**:
  - `int` → enteros
  - `double` → decimales
  - `char` → caracteres
  - `boolean` → valores lógicos (`true` / `false`)
  - Otros: `byte`, `short`, `long`, `float`

- **No primitivos**:
  - `String` → texto
  - Clases y objetos definidos por el programador

---

## 📖 Variables
- Una **variable** es un espacio en memoria donde se guarda un valor.  
- Se declara indicando tipo + nombre + valor:
  ```java
  int edad = 25;
  String nombre = "Lucas";
